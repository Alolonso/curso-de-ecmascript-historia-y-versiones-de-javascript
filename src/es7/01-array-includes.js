let numbers = [1, 3, 4, 6, 7, 8];

console.log(numbers.includes(4)); //True se encuentra en el array
console.log(numbers.includes(9));  //false no se encuentra en el array

const list = ["Oscar", "David", "Ana"]; 

console.log(list.includes("Oscar")); //True se encuentra en el array
console.log(list.includes("oscar")); //False no se encuentra en el array por pero david en el array esta la primera letra en mayuscula